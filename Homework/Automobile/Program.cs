namespace Automobile;

class Program
{
    static void Main(string[] args)
    {
        // Sebastian Main
        // Creating classes + constructors + enumerators

        // Create an instance of Automobile class

        // Automobile is to have the following private properties:
        // make (of type string)
        // model (of type string)
        // vin (of type string)
        // color (of type string)
        // year (of type int)
        // type (of type autoType)

        // TESTING
        Automobile Test = new Automobile("Volkswagen", "Jetta", "123456", "Black", 2014, "Sedan");


    }
}
